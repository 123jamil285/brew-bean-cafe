# Brew & Bean Cafe — Roadmap

## Done
- Brand identity, design system, pages, animation system
- Responsive system: container utility, fluid type scale, mobile-first grids,
  48px touch targets, full-screen glass mobile menu, no horizontal scroll
- Reusable component library in `src/components/lux` (layout, buttons, cards,
  forms, modal, marketing, feedback, motion wrappers)
- SEO, accessibility and structured data pass across all routes
- Code quality pass: shared content in `src/constants`, `useContactForm` hook,
  contact form validation + loading state, professional README

## Next (optional)
- [ ] Refactor remaining sections onto `lux` primitives where it removes duplication
- [ ] Sitemap once a public URL exists
